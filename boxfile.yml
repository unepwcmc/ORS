run.config:
  engine: ruby
  engine.config:
    runtime: ruby-2.2
  extra_packages:
    - ImageMagick-6.9
    - nodejs
  build_triggers:
    - Gemfile
    - Gemfile.lock

data.db:
  image: nanobox/postgresql:9.4

data.redis:
  image: nanobox/redis
