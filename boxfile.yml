run.config:
  engine.config:
    runtime: ruby-2.2
  extra_packages:
    - ImageMagick-6.9
  build_triggers:
    - Gemfile
    - Gemfile.lock

data.db:
  image: nanobox/postgresql:9.4

data.redis:
  image: nanobox/redis
